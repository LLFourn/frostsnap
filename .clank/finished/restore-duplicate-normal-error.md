# restore-duplicate-normal-error
Goal: correct the top-level restoration behavior exposed by the PR 4 GIF. In NewRestorationContext, Create or restore then Use an existing device key must report the ordinary duplicate or already-existing-wallet error when the device share access_structure_ref already exists locally, even if the stored encryption key cannot decrypt that wallet. find_share silently ignoring an undecryptable complete wallet is expected. Prefer reverting the special WalletKeyMismatch branch in check_start_restoring_key_from_device_share and the corresponding top-level Dart dialog handling if that cleanly restores the previous behavior. Preserve delete-and-recover handling for operations that must decrypt an existing wallet, including signing, backup inspection, and AddingToWalletContext. Update key_mismatch coverage to assert normal duplicate semantics for this top-level flow. Run focused Android fsim tests for key_mismatch, key_delete_recovery, key_pin_clear_recovery, and recover_lock_required plus relevant Dart analysis. After behavior is correct, capture slower evidence that clearly shows the navigation and resulting message for the PR body. Acceptance: top-level duplicate restoration has the same ordinary error with a correct, wrong, or unavailable local encryption key; decrypt-existing flows retain their recovery dialog; focused tests pass; replacement evidence is legible.